# Clarity Core Adoption Report Generator

## Usage

Execute the following command in your project:

```
npx github:sis0k0/core-adoption-report
```

The generated report will be saved in `report.html`.

